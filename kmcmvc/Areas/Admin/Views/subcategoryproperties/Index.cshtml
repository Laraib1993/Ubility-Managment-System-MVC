@model IEnumerable<kmcmvc.Areas.Admin.Data.subcategoryproperty>

@{
    ViewBag.Title = "Sub Category";
}

    <div class="pcoded-inner-content">
        <!-- Main-body start -->
        <div class="main-body">
            <div class="page-wrapper">
                <!-- Page-body start -->
                <div class="page-body">
                    <div class="row">
                        <div class="col-sm-12">
                            <div class="card">
                                <div class="card-header table-card-header">
                                    <h5>MUCT KMC Sub Category List</h5>

                                </div>
                                <div class="card-block">
                                    <span>To add new record @Html.ActionLink("Click on this link", "Create")</span>
                                    <div class="dt-responsive table-responsive">
                                        <table id="basic-btn" class="table table-striped table-bordered nowrap">
                                            <thead>
                                                <tr>
                                                    <th>Sub Category</th>
                                                    <th>Property Type</th>
                                                    <th>Property Code</th>
                                                    <th>Actions</th>
                                                </tr>
                                            </thead>
                                            <tbody>
                                            <tbody>
                                                @foreach (var item in Model)
                                                {

                                                <tr>
                                                    <td>
                                                        @Html.DisplayFor(modelItem => item.SubCategoryName)
                                                    </td>
                                                    <td>
                                                        @Html.DisplayFor(modelItem => item.PropertyTypeID)
                                                    </td>
                                                    <td>
                                                        @Html.DisplayFor(modelItem => item.subcategorycode)
                                                    </td>

                                                    <td>
                                                        @Html.ActionLink("Edit", "Edit", new { id = item.SubCategoryID }) |
                                                        @Html.ActionLink("Details", "Details", new { id = item.SubCategoryID }) |
                                                        @Html.ActionLink("Delete", "Delete", new { id = item.SubCategoryID })
                                                    </td>
                                                </tr>



                                                }
                                            </tbody>
                                            <tfoot>
                                                <tr>
                                                    <th>Sub Category</th>
                                                    <th>Property Type</th>
                                                    <th>Property Code</th>
                                                    <th>Actions</th>
                                                </tr>
                                            </tfoot>
                                        </table>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>

}
